[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  # IMPORTANT: Update this URL to match your Render backend URL once deployed
  NEXT_PUBLIC_API_URL = "https://watch-party-backend.onrender.com"
